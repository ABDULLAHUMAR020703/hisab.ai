# Supabase Edge Functions

Phase A placeholder. Planned functions (Phase E+):

| Function | Purpose |
|----------|---------|
| `zatca-onboard` | CSR generation + compliance CSID (OpenSSL) |
| `zatca-submit-invoice` | Invoice clearance/reporting |
| `zatca-renew-certificate` | Certificate renewal cron |

Deploy with Supabase CLI when ready:

```bash
supabase functions deploy zatca-onboard
```
